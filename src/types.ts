export interface HeaderLinkProps {
  name: string;
  link: string;
  icon: JSX.Element;
}

export interface LinkDataProps {
  name: string;
  link: string;
  icon: JSX.Element;
  className?: string;
}
